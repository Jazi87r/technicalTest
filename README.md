Como correr el proyecto:

1. clonar el repositorio
2. correr el comando "npm i" en la carpeta backend y también frontend para instalar las dependencias
3. en la carpeta frontend, correr el comando "npm run dev" e ir al host indicado en la consola

Arquitectura usada:

1. MVC (modelo, vista, controlador)
